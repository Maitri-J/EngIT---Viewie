import React from 'react'

import config from '../google-credentials.json';
import firebase from "firebase/app";
import "firebase/auth"; 

const FirebaseProvider = () => {
    return (
        <div>
            
        </div>
    )
}

export default FirebaseProvider
