import React from 'react'
import FirebaseProvider from './FirebaseProvider'

const Providers = () => {
    return (
        <FirebaseProvider>

        </FirebaseProvider>
    )
}

export default Providers
